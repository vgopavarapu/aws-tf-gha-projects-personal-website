name: Test

on: 
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  log_message:  
    runs-on: ubuntu-latest
    steps:
      - name: Log
        run: echo "495935757188:This workflow is working."
  